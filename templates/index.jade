doctype

include mixins.jade

html
    head
        meta(charset="utf-8")
        title= self.api.name || 'API Documentation'
        link(rel="icon", otype="image/x-icon", href="http://yunshequ00.oss-cn-hangzhou.aliyuncs.com/predefine/yun-she-qu.ico") 
        link(rel="shortcut icon", type="image/x-icon", href="http://yunshequ00.oss-cn-hangzhou.aliyuncs.com/predefine/yun-she-qu.ico") 
        link(rel="stylesheet", href="https://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css")
        style!= self.css
    body.preload
        div.api-header #{self.api.name}API文档 
        a.text-muted.back-to-top(id='gotoTop')
            i.fa.fa-toggle-up
            | &nbsp;Back to top
        div(class=self.fullWidth ? 'container-fluid api-container' : 'container-fluid api-container')
            .row
                block nav
                    +Nav(false)

                .content
                    block content
                        +Content('primary', false)

        script: include scripts.js
       
        if self.livePreview
            script(src="/socket.io/socket.io.js")
            script.
                var socket = io();
                socket.on('refresh', refresh);
                socket.on('reconnect', function () {
                    socket.emit('request-refresh');
                });
